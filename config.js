const  config = {
    port: 3000,
    database: {
        DATABASE: 'deenote',
        USERNAME: 'dee',
        PASSWORD: '123456',
        PORT:'3306',
        HOST:'127.0.0.1',
    }
}

module.exports = config